name: client_support_app
description: Internal client support and credential management app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  flutter_animate: ^4.3.0
  lucide_icons: ^0.257.0
  intl: ^0.19.0
  flutter_local_notifications: ^17.0.0
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  timeago: ^3.6.0
  percent_indicator: ^4.2.3
  shimmer: ^3.0.0
  url_launcher: ^6.2.2
  clipboard: ^0.1.3
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
